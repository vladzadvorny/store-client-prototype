import React from 'react';
import ReactLoading from 'react-loading';

export default () => (
  <div className="row" style={{ display: 'flex', justifyContent: 'center' }}>
    <ReactLoading type="spinningBubbles" color="#0088cc" />
  </div>
);
